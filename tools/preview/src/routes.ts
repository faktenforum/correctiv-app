/**
 * A hand-picked walk through `apps/mobile/src/app`, one entry per route shape.
 *
 * Not generated, so a new screen will not appear here on its own — the route
 * field takes any path, and whatever the frame navigates to is read back live.
 * Generating it from the router is an open question: `expo-router` serves
 * `/_sitemap` in a dev build, but the export has none, so a list that is honest
 * in both places has to come from the file tree at build time.
 */
export const ROUTES = [
  '/',
  '/entdecken',
  '/mediathek',
  '/mitmachen',
  '/profil',
  '/artikel',
  '/spotlight',
  '/suche',
  '/gespeichert',
  '/backstage',
  '/atlas',
  '/bericht',
  '/beitreten',
  '/einstellungen',
  '/faktenforum',
  '/formular',
  '/onboarding',
  '/player',
  '/video',
  '/aufruf/wem-gehoert-die-stadt',
  '/behauptung/claim-001',
  '/projekt/klima',
  '/serie/klima',
  '/tagebuch/diary-bern-2',
];

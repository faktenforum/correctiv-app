# Screens

Three pictures of the shell itself, shot against `npm run web` on 2026-09-01. Not a
record of the app — that is [`screens/`](../../../screens/README.md) — but of what
this tool looks like, so its README and a pull request can show rather than describe.

| | |
| --- | --- |
| `demo.webp` | plain: what the published Pages link shows |
| `tools.webp` | `Tools` on, with the checks run against `/entdecken` |
| `tokens.webp` | the same app after two colour tokens were overridden live |

{
  "name": "@correctiv/app-core",
  "description": "Platform-free core of the CORRECTIV app: models, feed/article parsing, HTTP, caching and framework-neutral stores. Imports no UI framework and no platform SDK — hosts supply capabilities via ports and bind the stores themselves.",
  "version": "1.0.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  }
}

{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      // jest-expo turns these into a moduleNameMapper, where the FIRST match wins
      // with no fallback — so "@/assets/*" has to be reinstated ahead of "@/*"
      // there. See the note in jest.config.js.
      "@/*": ["./src/*"],
      "@/assets/*": ["./assets/*"],
      // Point at the core's sources directly. It ships TypeScript, not a build,
      // so without these tsc cannot follow the subpath exports and reports every
      // @correctiv/app-core/* import as a missing module. Metro resolves them at
      // runtime through the workspace symlink and the package's exports map.
      "@correctiv/app-core": ["../../packages/app-core/src/index.ts"],
      "@correctiv/app-core/*": ["../../packages/app-core/src/*"]
    }
  },
  "include": ["**/*.ts", "**/*.tsx", ".expo/types/**/*.ts", "expo-env.d.ts", "nativewind-env.d.ts"],
  "exclude": ["node_modules", "android", "ios", "__tests__", "scripts"]
}

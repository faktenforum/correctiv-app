# The built site. CI builds it; nothing here is committed.
dist/

# Extracted by `npm run api` from packages/app-core, which the build runs first.
# Not committed: it is 144 KB of derived JSON, and regenerating it is one command
# rather than a merge conflict on every rename in the core.
content/api.generated.json
